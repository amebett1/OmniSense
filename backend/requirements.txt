# ============================================================
# OmniSense Backend - pip requirements
# ============================================================
# Cài đặt: pip install -r backend/requirements.txt
# ============================================================

# Web framework
flask>=3.0.0

# CORS support (cho phép web frontend gọi API)
flask-cors>=4.0.0

# Voice Assistant LLM & Environment
groq>=0.9.0
python-dotenv>=1.0.0

# Các thư viện từ pipeline chính (đã có trong requirements.txt gốc)
# numpy, opencv-python, insightface, onnxruntime, Pillow, tqdm

# RAG & Embeddings
chromadb>=0.4.0
PyMuPDF>=1.23.0
sentence-transformers>=2.3.0
rapidocr_onnxruntime>=1.3.0
python-docx>=1.1.0
pdf2image>=1.17.0
pytesseract>=0.3.10
